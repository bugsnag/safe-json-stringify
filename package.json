{
  "name": "@bugsnag/safe-json-stringify",
  "version": "6.0.0",
  "repository": {
    "type": "git",
    "url": "git@github.com:bugsnag/safe-json-stringify.git"
  },
  "files": [
    "index.js"
  ],
  "bugs": {
    "url": "https://github.com/bugsnag/safe-json-stringify/issues"
  },
  "homepage": "https://github.com/bugsnag/safe-json-stringify",
  "description": "Prevent defined property getters from throwing errors",
  "main": "index.js",
  "scripts": {
    "postversion": "git push && git push --tags",
    "preversion": "npm test",
    "test": "standard && karma start --single-run",
    "test:basic": "standard && karma start --single-run --browsers ChromeHeadless"
  },
  "author": "Debitoor",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^17.0.0",
    "jasmine-core": "^5.1.1",
    "karma": "^6.4.2",
    "karma-browserify": "^8.1.0",
    "karma-chrome-launcher": "^3.2.0",
    "karma-jasmine": "^5.1.0",
    "standard": "^17.1.0",
    "watchify": "^4.0.0"
  }
}
