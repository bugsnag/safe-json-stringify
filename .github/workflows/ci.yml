name: "unit test"

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
      - name: Checkout PR branch
        uses: actions/checkout@v4
      - run: npm install
      - run: npm test
